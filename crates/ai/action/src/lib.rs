// trueworld-action: Action recognition for TrueWorld
//
// This crate recognizes player actions from camera input.

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
