// renet-adapter: Renet network adapter for TrueWorld
//
// This crate provides a wrapper around Renet for client-server networking.

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
